<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.compatibilityapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PeopleActivity"></activity>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".Profile2Activity" />
        <activity android:name=".FinalActivity" />
        <activity android:name=".TheirQualitiesActivity" />
        <activity android:name=".TheirFeaturesActivities" />
        <activity android:name=".YourQualitiesSliderActivity" />
        <activity android:name=".YourQualitiesActivity" />
        <activity android:name=".GetToKnowActivity" />
        <activity android:name=".ChooseCelebrityActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>